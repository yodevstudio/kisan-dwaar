rules_version = '2';

// Deny-by-default. Opened per-user, per-path only inside S2.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
